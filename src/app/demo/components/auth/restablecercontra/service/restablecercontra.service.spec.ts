/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RestablecercontraService } from './restablecercontra.service';

describe('Service: Restablecercontra', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RestablecercontraService]
    });
  });

  it('should ...', inject([RestablecercontraService], (service: RestablecercontraService) => {
    expect(service).toBeTruthy();
  }));
});
