/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { OlvidocontraService } from './olvidocontra.service';

describe('Service: Olvidocontra', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [OlvidocontraService]
    });
  });

  it('should ...', inject([OlvidocontraService], (service: OlvidocontraService) => {
    expect(service).toBeTruthy();
  }));
});
